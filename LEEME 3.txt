OPERACIONES · ETAPA 2 · Clientes con onboarding + Tickets
════════════════════════════════════════════════════════

QUÉ HACE:
Dentro de Centro de Operaciones → pestaña Operaciones, ahora hay
una pantalla real con dos secciones:

- CLIENTES: lista de los clientes de NEXO que ya pagan, con plan,
  checklist de onboarding (Obra / Usuarios / Capacitación — tres
  botones que tildás con un toque), fecha del último uso (con un
  link "marcar uso hoy"), y estado (onboarding / activo / en_riesgo
  / perdido, con color).

- TICKETS: pedidos de soporte por cliente, con prioridad y estado
  (abierto / en_progreso / resuelto / cerrado).

Los dos con botón "+ Nuevo" y "Borrar" por fila. Todo en Supabase
(operaciones_clientes y operaciones_tickets).

Marketing también quedó del todo confirmado: "2 de 8" pasa a
"3 de 8 áreas conectadas" con Operaciones sumado.

⚠️ MUY IMPORTANTE — CÓMO SUBIR ESTE ARCHIVO:
Los últimos intentos fallaron porque el archivo se subió con
nombre duplicado o se editó con el lápiz de GitHub (que no soporta
archivos de este tamaño y los deja vacíos). Para que no vuelva a
pasar:

1. Bajá el index.html de este ZIP.
2. ANTES de ir a GitHub, andá a la app Archivos del iPad y
   confirmá que el nombre sea EXACTAMENTE "index.html" — sin
   espacios, sin números, sin ".html.html". Si tiene cualquier
   otra cosa, renombralo ahí mismo primero.
3. En GitHub: "Add file → Upload files" (NUNCA el lápiz/editar
   para este archivo).
4. Confirmá que la lista de "reemplazar" diga "index.html" y
   nada más.
5. Commit changes.

REQUISITO:
Necesita el SQL de operaciones_tablas.sql ya corrido en Supabase
(el que te pasé antes con las tablas de clientes y tickets).

VERIFICADO ANTES DE ENTREGAR:
- Sintaxis de JavaScript revisada con Node, sin errores.
- Bloque de Contratista comparado byte a byte: idéntico, no se tocó.
